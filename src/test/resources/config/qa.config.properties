browser = firefox

url = https://naveenautomationlabs.com/opencart/index.php?route=account/login

username = set88@gmail.com
password = Admin@123

headless = false
incognito = false

highlight = false




// If we are running test scripts from command line then we will directly send the browser, username, password
// from the command line dynamically.
// If we are running TC from command line then the browser, username, password etc. will not be captured from config.properties file.
// 
// we use command :  mvn clean install -Denv="qa" -Dbrowser="chrome" -Dusername="abc@gmail.com" -Dpassword="Admin@123"

// In case of command line execution we need to write : System.setProperty("headless") in OptionsManager class.